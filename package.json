{
  "name": "prism-action",
  "version": "1.0.0",
  "description": "Filters, groups, and color-codes repositories based on stars and recency.",
  "main": "dist/index.js",
  "scripts": {
    "package": "ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "keywords": [
    "github-actions",
    "repositories",
    "filter",
    "color",
    "gradient",
    "prism"
  ],
  "author": "Rutansh Govardhan",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}